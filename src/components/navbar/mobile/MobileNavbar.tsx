import { MobileNavbar_Bar, MobileNavbar_Menu } from "../..";

const MobileNavbar = () => {
  return (
    <nav>
      <MobileNavbar_Bar/>
      <MobileNavbar_Menu/>
    </nav>
  );
};

export default MobileNavbar;